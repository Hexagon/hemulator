[package]
name = "emu_n64"
version = "0.1.0"
edition = "2021"

[dependencies]
emu_core = { path = "../../core" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "2.0"
crc32fast = "1.4"

# OpenGL dependencies (required)
glow = "0.16"
bytemuck = "1.14"

[features]
default = []
